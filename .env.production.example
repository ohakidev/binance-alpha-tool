# ================================
# Production Environment Variables
# ================================
# Copy this file to .env for production deployment

# Database (Use production database URL)
DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# Admin Key for API operations (Generate a secure random key)
ADMIN_KEY="your-secure-admin-key-here"

# App URL (Your production domain - DO NOT use localhost)
NEXT_PUBLIC_APP_URL="https://binance-alpha-tool.vercel.app"

# ================================
# Telegram Configuration
# ================================
TELEGRAM_BOT_TOKEN="your-telegram-bot-token"
TELEGRAM_CHAT_ID="your-telegram-chat-id"

# ================================
# Cron Job Security
# ================================
# Secret key for cron job authentication (Generate a secure random key)
CRON_SECRET="your-cron-secret-key"

# ================================
# Optional: External APIs
# ================================
# Binance API (if needed for additional features)
# BINANCE_API_KEY=""
# BINANCE_API_SECRET=""

# ================================
# Vercel Specific (Auto-set by Vercel)
# ================================
# VERCEL_URL is automatically set by Vercel during deployment
# Do not set this manually

# ================================
# Production Notes
# ================================
# 1. Never commit .env file with real values to git
# 2. Set these environment variables in Vercel Dashboard:
#    - Settings > Environment Variables
# 3. For DATABASE_URL, use a production database service like:
#    - Vercel Postgres
#    - PlanetScale
#    - Supabase
#    - Railway
# 4. Generate secure keys using: openssl rand -base64 32
# 5. NEXT_PUBLIC_APP_URL must be a valid HTTPS URL (not localhost)
#    - Telegram inline buttons require valid public URLs
