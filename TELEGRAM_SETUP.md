# 📱 วิธีตั้งค่า Telegram Bot สำหรับ Binance Alpha Tool

## 📋 สารบัญ
1. [สร้าง Telegram Bot](#1-สร้าง-telegram-bot)
2. [หา Chat ID](#2-หา-chat-id)
3. [ตั้งค่าในโปรเจค](#3-ตั้งค่าในโปรเจค)
4. [ทดสอบการส่งข้อความ](#4-ทดสอบการส่งข้อความ)
5. [แก้ปัญหา](#5-แก้ปัญหา)

---

## 1. สร้าง Telegram Bot

### ขั้นตอนที่ 1: สร้างบอทผ่าน @BotFather

1. เปิด Telegram และค้นหา `@BotFather`
2. เริ่มคุยกับ BotFather โดยพิมพ์: `/start`
3. สร้างบอทใหม่โดยพิมพ์: `/newbot`
4. ตั้งชื่อบอท (เช่น: `Binance Alpha Tracker`)
5. ตั้ง username บอท (ต้องลงท้ายด้วย `bot` เช่น: `binance_alpha_bot`)
6. **คัดลอก Bot Token** ที่ BotFather ให้มา (รูปแบบ: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### ขั้นตอนที่ 2: เก็บ Bot Token ไว้ใช้งาน

```env
TELEGRAM_BOT_TOKEN="1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"
```

---

## 2. หา Chat ID

มี 3 วิธีในการหา Chat ID:

### วิธีที่ 1: ส่งข้อความไปหาบอทตัวเอง (แนะนำสำหรับทดสอบ) ⭐

1. เปิด Telegram และค้นหาบอทของคุณ (ตาม username ที่ตั้งไว้)
2. กด **Start** และส่งข้อความอะไรก็ได้ให้บอท (เช่น `/start` หรือ `hello`)
3. รัน script ด้านล่างเพื่อหา Chat ID:

```bash
node get-chat-id.js
```

4. Script จะแสดง Chat ID ของคุณ (จะเป็นตัวเลข เช่น `123456789`)
5. คัดลอก Chat ID และใส่ในไฟล์ `.env`:

```env
TELEGRAM_CHAT_ID="123456789"
```

### วิธีที่ 2: ใช้ @userinfobot

1. เปิด Telegram และค้นหา `@userinfobot`
2. ส่งข้อความอะไรก็ได้ให้บอท
3. บอทจะตอบกลับมาพร้อม Chat ID ของคุณ
4. คัดลอก Chat ID และใส่ในไฟล์ `.env`

### วิธีที่ 3: ส่งไปยัง Channel (สำหรับ Public/Private Channel)

#### สำหรับ Public Channel:

1. **สร้าง Public Channel** ใน Telegram
2. ตั้งชื่อ channel และ **ตั้ง username** ให้ channel (เช่น `binance_alphaTH`)
3. **เพิ่มบอทเป็น Administrator**:
   - เปิด Channel → คลิกที่ชื่อ channel
   - ไปที่ **Administrators**
   - กด **Add Administrator**
   - ค้นหาและเลือกบอทของคุณ
   - เปิดสิทธิ์ **Post Messages**
4. ใช้ username ใน `.env`:

```env
TELEGRAM_CHAT_ID="@binance_alphaTH"
```

#### สำหรับ Private Channel:

1. **สร้าง Private Channel** ใน Telegram
2. **เพิ่มบอทเป็น Administrator** (ตามขั้นตอนด้านบน)
3. **หา Chat ID ของ Channel**:
   - เพิ่มบอท `@userinfobot` เข้าไปใน channel
   - บอทจะส่งข้อความบอก Chat ID (รูปแบบ: `-1001234567890`)
   - ลบ @userinfobot ออกจาก channel
4. ใช้ Chat ID ใน `.env`:

```env
TELEGRAM_CHAT_ID="-1001234567890"
```

---

## 3. ตั้งค่าในโปรเจค

แก้ไขไฟล์ `.env` ในโฟลเดอร์โปรเจค:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN="YOUR_BOT_TOKEN_HERE"
TELEGRAM_CHAT_ID="YOUR_CHAT_ID_HERE"
TELEGRAM_LANGUAGE="th"
```

**ตัวอย่าง:**

```env
# ส่งไปหาตัวเอง (Private Chat)
TELEGRAM_BOT_TOKEN="8341430610:AAEed4BAZFbJjCUam_gQdURfM_Igq_24k78"
TELEGRAM_CHAT_ID="123456789"
TELEGRAM_LANGUAGE="th"

# หรือส่งไปยัง Public Channel
TELEGRAM_BOT_TOKEN="8341430610:AAEed4BAZFbJjCUam_gQdURfM_Igq_24k78"
TELEGRAM_CHAT_ID="@binance_alphaTH"
TELEGRAM_LANGUAGE="th"

# หรือส่งไปยัง Private Channel
TELEGRAM_BOT_TOKEN="8341430610:AAEed4BAZFbJjCUam_gQdURfM_Igq_24k78"
TELEGRAM_CHAT_ID="-1001234567890"
TELEGRAM_LANGUAGE="th"
```

---

## 4. ทดสอบการส่งข้อความ

### ทดสอบด้วย Script

รัน script ทดสอบ:

```bash
node test-telegram.js
```

**ผลลัพธ์ที่คาดหวัง:**

```
=== Telegram Bot Test ===
Token: ✅ Found
Chat ID: @binance_alphaTH

📤 Sending test message to: @binance_alphaTH

✅ Message sent successfully!
Check your Telegram channel: @binance_alphaTH
```

### ทดสอบในแอปพลิเคชัน

1. เปิดแอปพลิเคชันที่ `http://localhost:3000`
2. ไปที่หน้า **Airdrops**
3. คลิกปุ่ม **Test Telegram** (มุมบนขวา)
4. ถ้าเชื่อมต่อสำเร็จจะแสดง toast: ✅ "Telegram เชื่อมต่อแล้ว!"
5. คลิกปุ่ม **Send** ที่ airdrop ที่ต้องการส่ง
6. ตรวจสอบข้อความใน Telegram

---

## 5. แก้ปัญหา

### ❌ Error: "chat not found"

**สาเหตุ:**
- Channel ไม่มีอยู่จริง หรือชื่อไม่ถูกต้อง
- Bot ยังไม่ได้ถูกเพิ่มเป็น admin ใน channel
- ใช้ username (@xxx) กับ private channel

**วิธีแก้:**
1. ตรวจสอบว่า channel มีอยู่จริง
2. เพิ่ม bot เป็น admin ใน channel พร้อมสิทธิ์ "Post Messages"
3. ถ้าเป็น private channel ให้ใช้ Chat ID แทน username

### ❌ Error: "bot was blocked by the user"

**สาเหตุ:**
- คุณได้ block บอทใน Telegram

**วิธีแก้:**
1. เปิด Telegram และไปที่ Settings → Privacy and Security → Blocked users
2. หา bot ของคุณและ unblock

### ❌ Error: "ETELEGRAM: 401 Unauthorized"

**สาเหตุ:**
- Bot Token ไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบ TELEGRAM_BOT_TOKEN ในไฟล์ `.env`
2. ลอง revoke token และสร้างใหม่ที่ @BotFather

### ❌ ไม่แสดง Chat ID เมื่อรัน get-chat-id.js

**สาเหตุ:**
- ยังไม่ได้ส่งข้อความให้บอท

**วิธีแก้:**
1. รัน `node get-chat-id.js` ก่อน
2. **จากนั้น** ค่อยเปิด Telegram และส่งข้อความให้บอท
3. Chat ID จะแสดงใน terminal

### ⚠️ ข้อความส่งช้า

**สาเหตุ:**
- Network latency
- Telegram API rate limiting

**วิธีแก้:**
- ปกติ ไม่จำเป็นต้องแก้ไข
- ถ้าส่งบ่อยเกินไป อาจโดน rate limit (ประมาณ 30 ข้อความต่อวินาที)

---

## 📝 หมายเหตุ

- **Bot Token** ต้องเก็บเป็นความลับ ห้ามแชร์ให้ใคร
- **Chat ID** สามารถเปลี่ยนได้ตามต้องการ
- ถ้าใช้ Public Channel บอทต้องเป็น **Administrator**
- ถ้าใช้ Private Chat บอทต้อง**ไม่ถูก block**
- รองรับทั้ง **Private Chat**, **Public Channel**, และ **Private Channel**

---

## 🔗 ลิงก์ที่มีประโยชน์

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [BotFather Commands](https://core.telegram.org/bots#6-botfather)
- [node-telegram-bot-api](https://github.com/yagop/node-telegram-bot-api)

---

## 🎉 เสร็จสิ้น!

ตอนนี้คุณสามารถส่งการแจ้งเตือนไปยัง Telegram ได้แล้ว! 🚀

ถ้ายังมีปัญหา กรุณาตรวจสอบ:
1. Bot Token ถูกต้อง ✅
2. Chat ID ถูกต้อง ✅
3. Bot เป็น admin ใน channel (ถ้าส่งไปยัง channel) ✅
4. Bot ไม่ถูก block (ถ้าส่งไปหาตัวเอง) ✅
